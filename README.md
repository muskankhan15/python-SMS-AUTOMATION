# python-SMS-AUTOMATION
This Python script sends SMS messages using the Twilio API. After importing the Twilio Client and adding your Account SID, Auth Token, Twilio number, and receiver number, it creates a secure client connection and sends a custom text message directly to the specified phone number.
